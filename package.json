{
  "name": "elixir-mcp-discord",
  "version": "0.2.0",
  "private": false,
  "description": "A Discord bot powered entirely by the Elixir MCP server. Prompts, schedules and channels are files an operator owns; the code is a runner with no clan in it.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/index.js",
    "probe": "node src/probe.js",
    "routines": "node src/cli.js list",
    "try": "node src/cli.js try",
    "test": "node --test --import ./scripts/setup-tests.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.72.0",
    "discord.js": "^14.16.3",
    "dotenv": "^17.0.0"
  }
}
